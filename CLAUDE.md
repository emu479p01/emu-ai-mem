# Claude Code guidance

Follow `AGENTS.md`. This is the public Engine repository, not a memory vault. Never add
the local `memories/` migration source, transcripts, credentials, indexes, or vault
clones to Git.

